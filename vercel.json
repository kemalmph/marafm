{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 /opt/flutter && /opt/flutter/bin/flutter build web --release --web-renderer html",
  "outputDirectory": "build/web",
  "framework": null
}
